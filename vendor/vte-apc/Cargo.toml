# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.56.0"
name = "vte"
version = "0.11.0"
authors = [
    "Joe Wilm <joe@jwilm.com>",
    "Christian Duerr <contact@christianduerr.com>",
]
exclude = ["/.travis.yml"]
description = "Parser for implementing terminal emulators (zellij-vte-apc fork: adds Perform::apc_dispatch)"
documentation = "https://docs.rs/vte/"
readme = "README.md"
keywords = [
    "ansi",
    "vte",
    "parser",
    "terminal",
]
categories = [
    "parsing",
    "no-std",
]
license = "Apache-2.0 OR MIT"
repository = "https://github.com/alacritty/vte"
resolver = "2"

[dependencies.arrayvec]
version = "0.7.2"
optional = true
default-features = false

[dependencies.utf8parse]
version = "0.2.0"

[dependencies.vte_generate_state_changes]
version = "0.1.0"

[features]
default = ["no_std"]
nightly = ["utf8parse/nightly"]
no_std = ["arrayvec"]

# zellij-vte-apc fork: keep this vendored crate out of the outer zellij
# workspace; it is consumed only via [patch.crates-io].
[workspace]
